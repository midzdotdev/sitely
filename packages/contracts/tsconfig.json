{
	"extends": "../../tsconfig.base.json",
	"comment": "`dom` supplies the WHATWG `URL` type the re-exported `URLCodec` references (absent from base es2023); mirrors packages/url-codec.",
	"compilerOptions": {
		"lib": ["es2023", "dom"]
	},
	"include": ["src", "test"]
}
