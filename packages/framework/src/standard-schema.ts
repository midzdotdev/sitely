// Re-export the Standard Schema v1 types from `@sitely/schemas` (the canonical home).
// Kept as a stable internal alias so existing framework imports don't churn.
export type { StandardSchemaV1 } from "@sitely/schemas/standard-schema";
