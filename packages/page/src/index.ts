// @sitely/page — scaffold placeholder (spec: docs/plan/01-page.md).
// Implementation lands in its build ticket. This file only makes the package
// a resolvable ESM module so the workspace typecheck has something to resolve.
export {};
